#include <stdio.h>
#include <conio.h>
int main(){
   int a , b, sum;
   printf("hap hai so nguyen:");//xuất dữ liệu ra
   scanf("%d%d", &a, &b);
   sum = a+b;
   printf("tong hai so nguyen la: %d+%d=%d", a, b, a+b);

   return 0;
}